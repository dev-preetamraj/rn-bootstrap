## React Native starter with Expo Router and Nativewind

### Things needs to be changed after cloning
- Open `app.json` and update 'name', 'scheme' and 'slug' for your app
- Open `package.json` and update 'name' for your app
